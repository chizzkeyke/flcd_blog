import { GET_USER, GET_AUTH_USER } from 'redux/actions/constants'

export function getUser(data) {
    return {
        action: GET_USER,
        payload: data
    }
}

export function getAuthUser(data) {
    return {
        action: GET_AUTH_USER,
        payload: data
    }
}