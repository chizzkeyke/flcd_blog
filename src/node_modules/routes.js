import React from 'react'
import { Switch, Route } from 'react-router-dom'

import Auth from 'containers/Auth'
import HomePage from 'containers/HomePage'
import CreatePost from 'containers/CreatePost'
import PostsControl from 'containers/PostControl'
import UpdatePost from 'containers/UpdatePost'

export default function () {
   return (
      <Switch>
         <Route path='/' component={HomePage} exact />
         <Route path='/myposts' component={HomePage} />
         <Route path='/register' component={Auth} />
         <Route path='/login' component={Auth} />
         <Route path='/post/:slug' component={PostsControl} exact/>
         <Route path='/createNewPost' component={CreatePost} />
         <Route path='/updatePost/:slug' component={UpdatePost} exact/>
      </Switch>
   )
}